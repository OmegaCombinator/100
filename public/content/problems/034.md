# #34 Divergence of the Harmonic Series

Status: verified staged lemma, not the full divergence theorem.

## Harness result

- Run: `2026-04-30-top100-034`
- Session: `fa5cc609ad4e`
- Attempts: 1
- Proof file: `acornlib-top100/projects/top100/harness-runs/2026-04-30-top100-034/workspace/scratch.ac`
- Independent verification: OK with Acorn `0.1.68`

## Generated Acorn

```acorn
from nat import Nat
from real import Real
from real.real_series import partial, partial_tail, tail
from semiring.from_nat import from_nat

define harmonic(k: Nat) -> Real {
  Real.1 / from_nat[Real](k.suc)
}

theorem harmonic_partial_tail(m: Nat, n: Nat) {
  partial(harmonic, m + n) = partial(harmonic, m) + partial(tail(harmonic, m), n)
} by {
  partial_tail(harmonic, m, n)
}
```

## Interpretation

This confirms the current Acorn syntax and library path for harmonic terms over `Real`, real division, `from_nat[Real]`, `partial`, and `tail`. The next useful step is a dyadic block lower-bound lemma for harmonic sums.

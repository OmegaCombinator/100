# #74 The Principle of Mathematical Induction

Status: passed.

## Formalization

This is a complete Acorn wrapper proof for natural-number induction. It proves that for any predicate `p : Nat -> Bool`, if `p(0)` holds and `p(n)` implies `p(n.suc)` for every `n`, then `p(n)` holds for every natural number `n`.

## Harness result

- Run: `2026-05-01-top100-074-induction-gpt55`
- Session: `287551ff1307`
- Model: `gpt-5.5`
- Worker attempts: 1
- Independent verification: OK with Acorn `0.1.68`

## Acorn proof

```acorn
from nat.nat_base import Nat, alt_induction

/// Freek Top 100 #74: the principle of mathematical induction.
theorem top100_074_mathematical_induction(p: Nat -> Bool) {
    (p(Nat.0) and forall(n: Nat) { p(n) implies p(n.suc) })
    implies
    forall(n: Nat) { p(n) }
} by {
    alt_induction(p)
}
```


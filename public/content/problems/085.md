# #85 Divisibility by 3 Rule

Status: passed.

## Formalization

This is a complete Acorn proof of the digit-list version of the divisibility-by-3 rule. For any list of integer decimal digits, the value obtained by reading the digits from accumulator `0` is divisible by `Int.3` if and only if the recursive digit sum is divisible by `Int.3`.

The proof is stronger than the usual digit-restricted statement in one respect: it works for any `List[Int]`, not only digits in `0..9`.

## Verification

- Verification: `28/28 OK` with Acorn `0.1.68`
- Clean PR worktree verification:
  - `src/int/int_arith.ac`: OK
  - `src/zmod.ac`: OK
  - `src/int_digits.ac`: OK
  - `projects/top100/085_divisibility_by_3_rule.ac`: OK

## Acorn proof

```acorn
from int import Int
from int.int_base import add_zero_left
from int_digits import int_digit_sum, int_read_digits_acc, int_read_digits_acc_mod3_digit_sum
from list import List
from nat import Nat
from relation_basic import equivalence_flip, equivalence_step
from zmod import int_mod_rel, int_mod_rel_is_equivalence, int_mod_rel_zero_iff_int_three_divides

/// Freek Top 100 #85: divisibility by 3 is preserved by taking the digit sum.
theorem top100_085_divisibility_by_3_rule(digits: List[Int]) {
    Int.3.divides(int_read_digits_acc(digits, Int.0)) =
    Int.3.divides(int_digit_sum(digits))
} by {
    let n = int_read_digits_acc(digits, Int.0)
    let s = int_digit_sum(digits)

    int_read_digits_acc_mod3_digit_sum(digits, Int.0)
    add_zero_left(s)
    int_mod_rel(Nat.3, n, s)

    int_mod_rel_is_equivalence(Nat.3)
    if int_mod_rel(Nat.3, n, Int.0) {
        equivalence_flip[Int](int_mod_rel(Nat.3), n, s)
        int_mod_rel(Nat.3, s, n)
        equivalence_step[Int](int_mod_rel(Nat.3), s, n, Int.0)
        int_mod_rel(Nat.3, s, Int.0)
    }
    if int_mod_rel(Nat.3, s, Int.0) {
        equivalence_step[Int](int_mod_rel(Nat.3), n, s, Int.0)
        int_mod_rel(Nat.3, n, Int.0)
    }
    int_mod_rel(Nat.3, n, Int.0) = int_mod_rel(Nat.3, s, Int.0)

    int_mod_rel_zero_iff_int_three_divides(n)
    int_mod_rel_zero_iff_int_three_divides(s)
    Int.3.divides(n) = Int.3.divides(s)
}
```

## Supporting library additions

The clean proof branch adds reusable support in `int.int_arith`, `zmod`, and `int_digits`, including:

- `ten_sub_one_eq_nine`
- `three_mul_three_eq_nine`
- `int_mod_rel_ten_one_mod3`
- `int_read_mod3_step`
- `int_read_mod3_compatible`
- `int_mod_rel_zero_iff_int_three_divides`
- `int_read_digits_acc`
- `int_digit_sum`
- `int_read_digits_acc_mod3_digit_sum`

The current status is based on the complete divisibility theorem above.

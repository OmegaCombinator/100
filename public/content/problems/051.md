# #51 Wilson's Theorem

Status: passed.

## Harness Result

- Run: `2026-05-08-top100-051-wilson`
- Model: `gpt-5.5`
- Proof file: `acornlib-wilson-051/src/top100/theorem_051_wilsons_theorem.ac`
- Upstream PR: <https://github.com/acornprover/acornlib/pull/672>
- Independent verification: `4/4 OK` with Acorn `0.1.75`
- Strict replay: single-module `4/4 OK`, full library `52987/52987 OK`, `0 searches performed`

## Formalization

This is a complete Acorn proof of Wilson's theorem for natural-number prime
moduli. The public wrapper states the standard equivalence between primality and
Wilson's factorial congruence, excluding the degenerate modulus `1`:

```acorn
theorem top100_051_wilsons_theorem(p: Nat) {
    p != Nat.1 implies
        (p.is_prime = (p - Nat.1).factorial.congr_mod(p - Nat.1, p))
}
```

The proof adds a standard-library layer for normalized modular inverses, reduced
residue products, and the inverse-pairing argument. The key prime-direction
theorem is:

```acorn
theorem prime_imp_wilson_factorial_congr(p: Nat) {
    p.is_prime implies (p - Nat.1).factorial.congr_mod(p - Nat.1, p)
}
```

The converse proves that any nontrivial modulus satisfying Wilson's congruence
is prime:

```acorn
theorem wilson_factorial_congr_imp_prime(p: Nat) {
    p != Nat.1 and (p - Nat.1).factorial.congr_mod(p - Nat.1, p)
        implies p.is_prime
}
```

## Interpretation

This matches the classical Wilson theorem: for natural numbers other than `1`, a
modulus `p` is prime exactly when `(p - 1)!` is congruent to `-1`, represented as
`p - 1`, modulo `p`.

The proof was generated by AixBot, manually reviewed, and independently
verified with Acorn.

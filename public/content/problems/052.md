# #52 The Number of Subsets of a Set

Status: verified staged lemma, not the full powerset cardinality theorem.

## Harness result

- Run: `2026-04-30-top100-052`
- Session: `ee5e080ee24c`
- Attempts: 1
- Proof file: `acornlib-top100/projects/top100/harness-runs/2026-04-30-top100-052/workspace/scratch.ac`
- Independent verification: OK with Acorn `0.1.68`

## Generated Acorn

```acorn
from finite_set import FiniteSet, finite_set_subset_has_list, fs_from_list
from list.list_base import List

theorem finite_subset_has_list_repr[T](items: List[T], s: FiniteSet[T]) {
    items.contains_set(s.underlying_set) implies exists(subitems: List[T]) {
        fs_from_list(subitems) = s
    }
} by {
    finite_set_subset_has_list[T](items, s)
}
```

## Interpretation

This confirms that finite subsets can be represented by lists using the current `finite_set` API. The next step toward #52 is a counting result for all subsets of a finite set.

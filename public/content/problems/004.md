# #4 Pythagorean Theorem

Status: passed.

## Harness Result

- Run: `2026-05-08-top100-004-pythagorean`
- Model: `gpt-5.5`
- Proof file: `acornlib-perf-top100-004/src/top100/theorem_004_pythagorean_theorem.ac`
- Upstream PR: <https://github.com/acornprover/acornlib/pull/658>
- Independent verification: `125/125 OK` with Acorn `0.1.75`
- Strict replay: single-module `125/125 OK`, full library `52100/52100 OK`, `0 searches performed`

## Formalization

This is a complete Acorn proof of the Pythagorean theorem in the real coordinate
plane. The proof defines concrete `R2` operations on `Pair[Real, Real]`: vector
addition, negation, subtraction, dot product, squared Euclidean norm, and
orthogonality.

The final public theorem follows the HOL-style orientation of the right-angle
hypothesis, using the vector from `b` to `a` and the vector from `b` to `c`:

```acorn
theorem top100_004_pythagorean_theorem(
    a: Pair[Real, Real], b: Pair[Real, Real], c: Pair[Real, Real]
) {
    r2_orthogonal(r2_sub(a, b), r2_sub(c, b)) implies
    r2_norm_sq(r2_sub(c, a)) = r2_norm_sq(r2_sub(b, a)) + r2_norm_sq(r2_sub(c, b))
}
```

## Interpretation

This matches the standard coordinate-plane form of Pythagoras: if the two legs
meeting at `b` are orthogonal, then the squared length of the side from `a` to
`c` is the sum of the squared lengths of the two legs.

The proof was generated by AixBot, manually reviewed, and independently
verified with Acorn.

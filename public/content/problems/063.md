# #63 Cantor's Theorem

Status: passed.

## Harness result

- Run: `2026-05-07-top100-063-cantor-wrapper`
- Model: `gpt-5.5`
- Proof file: `harness-local-runs/2026-05-07-top100-063-cantor-wrapper/artifacts/top100_063_cantor_theorem.ac`
- Independent verification: `12/12 OK` with Acorn `0.1.72`

## Formalization

This is a complete Acorn proof of Cantor's theorem in its diagonal form: for any
type `T`, no function from `T` to `Set[T]` is surjective.

The proof defines the diagonal set containing exactly those `x` for which
`f(x)` does not contain `x`, then derives a contradiction from any claimed
preimage of that diagonal set.

The final public theorem is:

```acorn
theorem top100_063_cantor_theorem[T](f: T -> Set[T]) {
    not is_surjective_fn[T, Set[T]](f)
} by {
    let diagonal: Set[T] = Set[T].new(function(x: T) { not f(x).contains(x) })
    if is_surjective_fn[T, Set[T]](f) {
        surjective_fn_has_preimage[T, Set[T]](f, diagonal)
        let a: T satisfy { f(a) = diagonal }
        if diagonal.contains(a) {
            diagonal.contains(a) = not f(a).contains(a)
            f(a).contains(a) = diagonal.contains(a)
            not diagonal.contains(a)
            false
        }
        if not diagonal.contains(a) {
            diagonal.contains(a) = not f(a).contains(a)
            f(a).contains(a) = diagonal.contains(a)
            not f(a).contains(a)
            diagonal.contains(a)
            false
        }
        false
    }
}
```

## Interpretation

This matches Cantor's theorem that the powerset of a set has strictly larger
cardinality than the set itself: in particular, no map onto all subsets can be
surjective.

The proof was generated by AixBot, manually reviewed, and independently
verified with Acorn.
